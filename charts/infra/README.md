### Network Resources on AWS
This chart deploys network resources on AWS using the AWS Controllers for Kubernetes (ACK) framework.
 It includes VPC, subnets, security groups, and other related resources.

